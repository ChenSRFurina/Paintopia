import SwiftUI

struct DrawingView: View {
    var body: some View {
        VStack {
            Text("绘画页（Canvas 占位）")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    DrawingView()
} 